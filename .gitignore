node_modules/
output/
fsh-generated/
temp/
template/
*.tgz
dist/
.DS_Store
input-cache/
txCache/

# Beads / Dolt files (added by bd init)
# .beads/ as a whole is NOT ignored — issues.jsonl + config must be tracked
# so collaborators can see the issue database. Dolt internals, sockets and
# local-only files are excluded by .beads/.gitignore.
.dolt/
*.db
.beads-credential-key

# Claude session logs
logs/

# Claude Code project config (keep commands/ tracked)
.claude/*
!.claude/commands/

# IG Publisher artifacts in root
Requirements-fromNarrative.json

# Draft documents (not ready for publication)
docs/white-paper-ihe-dental-europe.md
docs/handover-dental-taxonomy.md
docs/adr/ADR-004-companion-handover.md

# session-close transient artifacts (per-worktree IPC files)
.session-close.lock
.worktree-handoff.json

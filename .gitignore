node_modules/
output/
fsh-generated/
temp/
template/
*.tgz
dist/
.DS_Store
input-cache/
txCache/

# Beads task tracker — internal, not part of the IG.
# Data lives on the Dolt remote (dolt.cognovis.de); local copy stays untracked.
.dolt/
*.db
.beads-credential-key
.beads/

# Claude session logs
logs/

# Claude Code project config (keep commands/ tracked)
.claude/*
!.claude/commands/

# IG Publisher artifacts in root
Requirements-fromNarrative.json

# Draft documents (not ready for publication)
docs/white-paper-ihe-dental-europe.md
docs/handover-dental-taxonomy.md
docs/adr/ADR-004-companion-handover.md

# session-close transient artifacts (per-worktree IPC files)
.session-close.lock
.worktree-handoff.json
.wave-orchestrator.lock

# Non-IG tooling — kept locally but not tracked on the public IG repo.
# Agent/skill tooling, release orchestration, local dev convenience.
AGENTS.md
cliff.toml
docker-compose.yaml
